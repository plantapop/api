asyncapi: 2.0.0
info:
  version: 0.1.0
  title: Async API PlantaPop!
channels:
  'chat/{id}':
    $ref: 'paths/chat/chat.yml'
  'chat/{id}/receipts':
    $ref: 'paths/chat/receipt.yml'

components:
  securitySchemes:
    bearerAuth:
      $ref: 'components/securitySchemes/bearerAuth.yml'